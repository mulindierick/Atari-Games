# self balancing trees
# balanced binary search tee (difference btn siblings is at max 1) if diff siblings > 1, rotate tree to balance 
# height <= 1.44log(n) - 1.328 = worst case log(n)
# prove everything by induction